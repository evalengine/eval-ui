/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/eva-ui",
    output: "export",
    // reactStrictMode: true,
    images: {
        unoptimized: true,
      },
};

export default nextConfig;
